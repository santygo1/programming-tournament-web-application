import React from 'react';
import classes from "./TaskCreateFormPage.module.css";

const TaskCreateFormPage = (props) => {
    return (
        <div className={classes.TaskCreateFormPage}>

        </div>
    );
};

export default TaskCreateFormPage;